{% extends 'base.html.twig' %}

{% block title %}PublicItem{% endblock %}

{% block body %}
    <h1>PublicItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ public_item.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ public_item.name }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ public_item.size }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ public_item.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_public_item_index') }}">back to list</a>

    <a href="{{ path('app_public_item_edit', {'id': public_item.id}) }}">edit</a>

    {{ include('public_item/_delete_form.html.twig') }}
{% endblock %}
